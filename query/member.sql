--CREATE USER CONO IDENTIFIED BY CONO;
--GRANT RESOURCE, CONNECT TO CONO;


DROP TABLE MEMBER;
CREATE TABLE MEMBER(
	ID VARCHAR2(10) PRIMARY KEY,
	PW VARCHAR2(20) NOT NULL,
	NAME VARCHAR2(10) NOT NULL,
	PHONE VARCHAR2(15) NOT NULL,
	BIRTH DATE NOT NULL,
	GENDER VARCHAR2(3) NOT NULL,
	ADDRESS VARCHAR2(30),
	EMAIL VARCHAR2(20),
	GRADE VARCHAR2(9) DEFAULT '일반', -- 일반, 사장님, 관리자, 휴면 , 블랙
	STATUS VARCHAR2(1) DEFAULT 'Y',
	REGDATE DATE DEFAULT SYSDATE,
	MODIFYDATE DATE DEFAULT SYSDATE
);

INSERT INTO MEMBER VALUES('CHLTJDFKR', 'CHLTJDFKR', '최성락', '010-0101-0101', SYSDATE, '남', NULL, NULL, '관리자', DEFAULT ,SYSDATE, DEFAULT);
INSERT INTO MEMBER VALUES('CSL931', 'TJDFKR1!', '최성락', '010-0101-0101', SYSDATE, '여', '서울시', 'CSL931@GMAIL.COM', '관리자', DEFAULT ,SYSDATE, DEFAULT);

SELECT * FROM MEMBER WHERE STATUS = 'Y';

SELECT ID, NAME, PHONE, BIRTH, GENDER, ADDRESS, EMAIL, GRADE, STATUS, REGDATE, MODIFYDATE FROM MEMBER;

UPDATE MEMBER SET NAME = '' WHERE ID = '';
-- 뷰 생성 시 활용
SELECT ID, NAME, PHONE, BIRTH, GENDER, NVL(ADDRESS, '-'), NVL(EMAIL, '-'), GRADE, STATUS, REGDATE, NVL(MODIFYDATE, '-')
FROM MEMBER WHERE STATUS = 'Y';

commit;